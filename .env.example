# Copy this file to .env and fill in your actual API keys
# DO NOT commit .env to git (it's listed in .gitignore)

# Groq Translation (Free, recommended)
GROQ_API_KEY=your-groq-api-key-here

# Azure OpenAI (Optional, $200 free trial)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-4-turbo

# OpenAI (Optional, paid)
OPENAI_API_KEY=your-openai-api-key-here

# Ollama (Optional, local/free)
OLLAMA_BASE_URL=http://localhost:11434

# Background ambience mixing (optional)
# By default, the pipeline mixes a ducked version of original audio under the dub.
# Enable Demucs to extract a background-only (no vocals) bed first:
DEMUCS_BACKGROUND=1
DEMUCS_MODEL=htdemucs
DEMUCS_DEVICE=cpu   # or cuda
